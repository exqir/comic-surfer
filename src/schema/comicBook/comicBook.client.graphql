extend type Query {
  getComicBook(id: ID!): ComicBook
}

type ComicBook {
  _id: ID!
  title: String!
  issue: String
  releaseDate: Int
  # TOOD: Check what find returns if no items was found
  creators: [Creator!]
  series: ComicSeries
  publisher: Publisher
  coverUrl: String
  url: String!
}
