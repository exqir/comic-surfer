extend type Query {
  comicBook(id: ID!): ComicBook
}

type ComicBook {
  _id: ID!
  title: String!
  issueNo: String
  releaseDate: Date
  creators: [Creator!]!
  comicSeries: ComicSeries
  publisher: Publisher
  coverImgUrl: String
  url: String!
}
